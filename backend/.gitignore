# Environment variables (already covered, but let's be comprehensive)
.env
.env.local
.env.development
.env.test
.env.production
.env.*.local

# Database files
*.db
*.sqlite
*.sqlite3

# Logs (already covered, but let's be specific)
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories (already covered)
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
public

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Editor directories and files (already covered)
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files (already covered)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Prisma (already covered, but let's be specific)
/prisma/migrations
/prisma/seed.ts
/prisma/seed.dev.ts
/prisma/seed.prod.ts
/prisma/seed.*.ts

# Build outputs (already covered)
dist/
build/

# Package lock (already covered)
package-lock.json

# SSL certificates
*.pem
*.key
*.crt
*.csr
*.p12
*.pfx

# Docker
.dockerignore

# Fly.io
fly.toml.backup